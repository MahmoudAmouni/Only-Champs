import { PlaceholderPage } from "@/components/shared/placeholder-page";

export default function ChatPage() {
  return (
    <PlaceholderPage
      title="Chat"
      phase="Phase 8 — see docs/05-BUILD-ORDER.md"
      description="Direct or group thread with the coach, depending on tier — level-1 clients see an upgrade card here instead of a dead end. Spec: docs/04-FRONTEND.md '/chat'."
    />
  );
}
