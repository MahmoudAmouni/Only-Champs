import { PlaceholderPage } from "@/components/shared/placeholder-page";

export default function FeedPage() {
  return (
    <PlaceholderPage
      title="Feed"
      phase="Phase 5 — see docs/05-BUILD-ORDER.md"
      description="Tier-gated post feed — the signature screen. Unlocked posts render in full; locked ones show the blurred-thumbnail overlay with an upgrade CTA. Spec: docs/04-FRONTEND.md '/feed'."
    />
  );
}
