import { PlaceholderPage } from "@/components/shared/placeholder-page";

export default function ClientsPage() {
  return (
    <PlaceholderPage
      title="Clients"
      phase="Phase 4 — see docs/05-BUILD-ORDER.md"
      description="Full client roster with search, tier and status filters, sortable columns, and adherence bars. Spec: docs/04-FRONTEND.md '/clients'."
    />
  );
}
