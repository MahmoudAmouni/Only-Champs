import { PlaceholderPage } from "@/components/shared/placeholder-page";

export default function ContentPage() {
  return (
    <PlaceholderPage
      title="Content"
      phase="Phase 4 — see docs/05-BUILD-ORDER.md"
      description="Post grid with tier badges and a composer with the tier-radio selector showing live subscriber counts. Spec: docs/04-FRONTEND.md '/content' and '/content/new'."
    />
  );
}
