import { PlaceholderPage } from "@/components/shared/placeholder-page";

export default function SettingsPage() {
  return (
    <PlaceholderPage
      title="Settings"
      phase="Phase 4 — see docs/05-BUILD-ORDER.md"
      description="Profile, tiers, payments (Stripe Connect status), and account tabs. Spec: docs/04-FRONTEND.md '/settings'."
    />
  );
}
