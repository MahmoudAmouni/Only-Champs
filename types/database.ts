/**
 * PLACEHOLDER — replace by running, after the Supabase project from
 * docs/01-DATABASE.md is created and migrated:
 *
 *   supabase gen types typescript --linked > types/database.ts
 *
 * Never hand-edit the generated file. This stub exists only so that
 * lib/supabase/*.ts and anything importing `Database` type-checks before
 * a real project is linked.
 */
export type Database = {
  public: {
    Tables: Record<string, never>;
    Views: Record<string, never>;
    Functions: Record<string, never>;
    Enums: Record<string, never>;
  };
};
